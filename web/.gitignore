node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# Mock-mode profile for the "web-mocks" launch configuration. Holds no secrets:
# it only forces the fixture layer on, so it is tracked like .env.example.
!.env.mocks

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*storybook.log
storybook-static
