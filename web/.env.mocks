# Isolated profile for UI verification against the mock fixture layer, loaded only by
# `vite dev --mode mocks` (the "web-mocks" launch configuration). Leaves `.env` alone,
# so the normal dev server keeps pointing at the live API.
VITE_USE_MOCKS=true
VITE_API_URL=
